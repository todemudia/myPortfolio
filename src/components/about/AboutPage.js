import React from "react";

//You don't need a return key word here because you are using an arrow function
//With a parenthesis?
const AboutPage = () => (
    <div>
        <h2>About</h2>
        <p>
            This app uses React, Redux, React Router, and many other helpful libraries.
        </p>
    </div>
);

export default AboutPage;